// Placeholder MCP server entrypoint. Replaced wholesale during phase P1 of the
// migration plan, which copies the framework runtime from sui-mcp-course.
//
// Until P1 lands, this file exists only so that `pnpm build` (esbuild) and
// `tsc --noEmit` succeed in CI and developer environments.

export {};
