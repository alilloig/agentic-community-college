.DS_Store
*.log

# Personal session memory (not part of the project artifact)
.remember/

# Each lesson's seeded reference-app pulls deps locally
**/node_modules/
**/.vite/
**/dist/

# Per-project ACC state when this repo is itself used as a projectRoot
.acc/
